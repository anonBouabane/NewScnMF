import 'package:flutter/material.dart';
class Stycolors {
 static const darkblue = Color.fromARGB(255, 15, 106, 180);
static const lightblue = Color.fromARGB(255, 48, 179, 240);
static const background = Color.fromARGB(255, 25, 80, 174);

  
}
